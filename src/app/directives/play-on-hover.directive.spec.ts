import { PlayOnHoverDirective } from './play-on-hover.directive';

describe('PlayOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new PlayOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
